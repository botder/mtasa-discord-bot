{
    "relay-http-port": 8100,
    "relay-password": "key",
    "discord-token": "bot.token",
    "discord-guilds": [
        {
            "server-id": "server.id",
            "channels": [ "race", "dd", "dm" ]
        },
        {
            "server-id": "server.id",
            "channels": [ "freeroam", "race", "deathmatch" ]
        }
    ]
}
